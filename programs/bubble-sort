import("programs/funcs/bubble-sort");

array = [25, 22, 30, 18, 29, 21, 27, 21];
println(array);
start = time();
iters = bubble_sort_debug(array);
println("Sorting took " + iters + " iterations taking " + (time() - start) + " ms");
println(array);